% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{disappr_api}
\alias{disappr_api}
\alias{disappr_mapping}
\alias{disappr_prepare}
\alias{disappr_fit}
\alias{disappr_compare}
\alias{disappr_predict}
\alias{disappr_diagnose}
\alias{disappr_interpret}
\alias{disappr_code}
\alias{disappr_simulate}
\alias{disappr_read}
\alias{disappr_example}
\alias{disappr_provenance}
\title{Scripted disappR analyses}
\usage{
disappr_mapping(...)

disappr_prepare(data, mapping, dup_action = "keep")

disappr_fit(
  x,
  models = MODEL_IDS,
  age_function = "Quadratic",
  family = "gaussian",
  ...
)

disappr_compare(fit)

disappr_predict(fit, model, ages = NULL, by = NULL)

disappr_diagnose(fit)

disappr_interpret(fit)

disappr_code(fit, x, source_label = "your_data.csv")

disappr_simulate(...)

disappr_read(path)

disappr_example(key)

disappr_provenance(fit)
}
\arguments{
\item{...}{For \code{disappr_mapping()}: column roles (\code{id}, \code{age}, \code{trait}, \code{alr}, \code{life}, \code{entry}, \code{covars},
\code{cov_factor}, \code{cov_age}, \code{group}, \code{group2}, \code{random}, ...). For \code{disappr_fit()}: further settings passed to the
model suite (\code{random_slope}, \code{standardise}, \code{zi_str}, \code{among}, \code{include_invalid}, \code{extra}). For
\code{disappr_simulate()}: simulation settings (\code{n_id}, \code{seed}, \code{sd_type}, \code{missingness}, \code{form}, ...).}

\item{data}{A data frame with one row per record.}

\item{mapping}{A column mapping from \code{disappr_mapping()}.}

\item{dup_action}{What to do with repeated individual-by-age records: \code{"keep"} or \code{"mean"}.}

\item{x}{Prepared data from \code{disappr_prepare()}.}

\item{models}{Model identifiers, \code{"M1"} to \code{"M10"}.}

\item{age_function}{Ageing function: \code{"Linear"}, \code{"Quadratic"}, \code{"Cubic"}, \code{"Logarithmic"} or
\code{"Asymptotic exponential"}.}

\item{family}{Error family, e.g. \code{"gaussian"}, \code{"poisson"}, \code{"nbinom2"}, \code{"binomial"}.}

\item{fit}{A fitted comparison from \code{disappr_fit()}.}

\item{model}{One model identifier, e.g. \code{"M4"}.}

\item{ages}{Ages at which to predict (default: a fine grid over the observed ages).}

\item{by}{Optional categorical covariate whose levels get separate predictions.}

\item{source_label}{File name the exported script reads.}

\item{path}{Path to a CSV or other delimited text file.}

\item{key}{Name of a bundled example, e.g. \code{"bouwhuis"} (see \code{names(disappR:::EXAMPLES)}).}
}
\value{
\code{disappr_mapping()}: a mapping list. \code{disappr_prepare()}: a list with \code{data} and \code{meta}.
\code{disappr_fit()}: the model comparison (AIC table, coefficients, fits, validity). \code{disappr_compare()}: the AIC
table. \code{disappr_predict()}: predicted population trajectory. \code{disappr_diagnose()}: validity, status, dropped
rows and notes. \code{disappr_interpret()}: interpretation notes. \code{disappr_code()}: an R script as a string.
\code{disappr_simulate()}: a simulated data set.
}
\description{
The functions the Shiny app uses, available for analyses in R scripts: build a column mapping, prepare the data,
fit and compare Models 1-10, predict population trajectories, and read the diagnostics, the interpretation notes
and the reproducible code.
}
\examples{
\dontrun{
sim <- disappr_simulate(n_id = 150, seed = 1, sd_type = "both")
map <- disappr_mapping(id = "ID", age = "age", trait = "body_mass", life = "lifespan")
x <- disappr_prepare(sim, map)
fit <- disappr_fit(x, models = c("M1", "M2", "M4"))
disappr_compare(fit)
}
}
