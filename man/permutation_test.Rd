% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effects.R
\name{permutation_test}
\alias{permutation_test}
\title{Permutation test for selective disappearance}
\usage{
permutation_test(
  dat,
  meta,
  model = "M4",
  against = "M1",
  n_perm = 100,
  settings = list(),
  progress = NULL,
  seed = 1L
)
}
\arguments{
\item{dat}{Standardised data.}

\item{meta}{Mapping metadata.}

\item{model}{The model to test (for example "M4").}

\item{against}{The reference model, usually "M1".}

\item{n_perm}{Number of permutations.}

\item{settings}{A list of fitting settings (family, age_function, random_slope, standardise, zi, among, extra).}

\item{progress}{Optional function(fraction, message) for a progress bar.}

\item{seed}{Seed for the permutations; the session's random-number stream is restored afterwards.}
}
\value{
A list with the observed statistics, the null draws and one-sided p-values.
}
\description{
Breaks the link between each individual's lifespan proxy and its own trait records by permuting the
proxy across individuals, then refits. Everything else - the ageing pattern, the repeated-measures
structure, the missingness, the number of records per individual - is left exactly as it is. Under this
null there is no selective disappearance, so the distribution of the AIC advantage across permutations
is what the chosen model can win by through chance and model flexibility alone.
}
